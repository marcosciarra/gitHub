help comando --> esce l'help del comando
man comando --> esce il manuale del comando
info comando --> esce il manuale del comando (sudo apt info)

è possibile dichiarare variabili:
variabile="aaa"
echo $variabile

alias --> alias elenco=ls .lah --color

history --> mostra la storia dei comandi (Ctrl+R per ricerca)

mkdir --> crea directory
rmdir --> elimino directory

echo file* -->tutti i file che iniziano con file
echo file? -->tutti i file che iniziano con file ed hanno un solo carattere dopo


INSTALLARE / DISINSTALLARE UN PACCHETTO
apt install PACCHETTO
apt remove PACCHETTO


comandi vari 
pwd --> mostra il percorso della dir in cui siamo

